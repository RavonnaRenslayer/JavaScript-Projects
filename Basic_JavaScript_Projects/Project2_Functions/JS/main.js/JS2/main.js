//Acess the element by its ID
const messageElement = document.getElementById("message");

// Concatenate using +=
messageElement.textContent += "The text has changed!";

//Execute JavaScript function
